# Find a point group

*Coming in Phase D3. Tracking placeholder.*

Will cover when to use `pointGroup_simple` (validation), `pointGroup_fast` (clean inputs), `pointGroup_robust` (real data), and how to choose tolerance.
