# Compose and apply operations

*Coming in Phase D3. Tracking placeholder.*

Will cover `SpacegroupOp` operator overloads (composition, inverse, application to a fractional position) and getting a Cartesian form via `toCartesian`.
