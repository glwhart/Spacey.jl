# Detect tolerance-dependent answers

*Coming in Phase D3. Tracking placeholder.*

Will cover `verify_stable=true` on both `pointGroup_robust` and `spacegroup`: when the warning fires and what to do about it.
