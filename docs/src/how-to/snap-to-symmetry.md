# Snap a noisy lattice to symmetry

*Coming in Phase D3. Tracking placeholder.*

Will cover `snapToSymmetry_SVD` (the public, recommended path) and the internal alternative `Spacey.snapToSymmetry_avg`, including the trade-offs (SVD is more robust at high distortion; the avg variant is faster but can drift) and volume preservation.
