# Space groups

*Coming in Phase D2. Tracking placeholder.*

Will document `spacegroup`, `isSpacegroupOp`, the `SpacegroupOp` struct and its methods (composition, inverse, callable, equality), and `toCartesian`.
